@php $client = $getRecord(); $invoices = $client ? $client->invoices()->latest('invoice_date')->get() : collect(); @endphp
<div>@if($invoices->isEmpty())<p class="text-center py-6 text-gray-500">Aucune facture.</p>
@else<div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50"><tr><th class="px-3 py-2 text-left text-xs">N</th><th class="px-3 py-2 text-left text-xs">Date</th><th class="px-3 py-2 text-left text-xs">HT</th><th class="px-3 py-2 text-left text-xs">TVA</th><th class="px-3 py-2 text-left text-xs">TTC</th><th class="px-3 py-2 text-left text-xs">Statut</th></tr></thead><tbody class="divide-y">
@foreach($invoices as $inv)<tr><td class="px-3 py-2 font-mono text-xs">{{ $inv->invoice_number }}</td><td class="px-3 py-2 text-xs">{{ $inv->invoice_date?->format('d/m/Y') }}</td><td class="px-3 py-2">{{ number_format($inv->subtotal,2,',',' ') }} &euro;</td><td class="px-3 py-2">{{ number_format($inv->tax_amount,2,',',' ') }} &euro;</td><td class="px-3 py-2 font-medium">{{ number_format($inv->total,2,',',' ') }} &euro;</td><td class="px-3 py-2"><span class="px-2 py-0.5 rounded-full text-xs {{ $inv->status==='paid'?'bg-green-100 text-green-800':($inv->status==='overdue'?'bg-red-100 text-red-800':'bg-gray-100 text-gray-600') }}">{{ match($inv->status){'paid'=>'Payee','sent'=>'Envoyee','draft'=>'Brouillon','overdue'=>'En retard','cancelled'=>'Annulee',default=>$inv->status} }}</span></td></tr>@endforeach
</tbody><tfoot class="bg-gray-50 font-medium"><tr><td colspan="4" class="px-3 py-2 text-right">Total</td><td class="px-3 py-2">{{ number_format($invoices->sum('total'),2,',',' ') }} &euro;</td><td></td></tr></tfoot></table></div>@endif</div>
