@extends('layouts.app')@section('title',$magazine->name)@section('content')
<div class="container-fluid py-8"><div class="flex items-center gap-4 mb-8"><a href="{{ route('client.dashboard') }}" class="text-gray-500 hover:text-gray-700">&larr; Retour</a><h1 class="text-2xl font-bold">{{ $magazine->name }}</h1></div>@if($issues->count()>0)<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 2xl:grid-cols-8 gap-4">@foreach($issues as $issue)<a href="{{ route('client.issue.read',$issue) }}" class="group bg-white rounded-xl shadow-sm border overflow-hidden hover:shadow-md">@if($issue->thumbnail_url)<img src="{{ $issue->thumbnail_url }}" class="w-full h-48 object-cover">@else<div class="w-full h-48 bg-gradient-to-br from-primary-400 to-primary-600 flex items-center justify-center"><span class="text-white font-bold">N{{ $issue->issue_number }}</span></div>@endif<div class="p-3"><p class="font-semibold text-sm group-hover:text-primary-600">N{{ $issue->issue_number }}</p><p class="text-xs text-gray-600">{{ $issue->title }}</p></div></a>@endforeach</div>@else<div class="bg-white rounded-xl border p-8 text-center text-gray-500">Aucun numero disponible.</div>@endif</div>
@endsection
