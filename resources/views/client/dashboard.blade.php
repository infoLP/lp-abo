@extends('layouts.app')@section('title','Mon espace')@section('content')
<div class="container-fluid py-8"><div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8 gap-4"><div><h1 class="text-2xl font-bold">Bonjour {{ $client->first_name }}</h1><p class="text-gray-600">{{ $client->client_number }}@if($client->is_payer) <span class="ml-2 px-2 py-0.5 bg-amber-100 text-amber-800 rounded-full text-xs">Compte payeur</span>@endif</p></div><div class="flex gap-3"><a href="{{ route('client.profile') }}" class="px-4 py-2 border rounded-lg text-sm">Profil</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="px-4 py-2 text-red-600 hover:bg-red-50 rounded-lg text-sm">Deconnexion</button></form></div></div>
@if(isset($payerAccount)&&$payerAccount)<div class="bg-amber-50 border border-amber-200 rounded-xl p-4 mb-6"><p class="text-amber-800 text-sm"><strong>Gere par :</strong> {{ $payerAccount->full_name }}</p></div>@endif
<div class="mb-8"><h2 class="text-lg font-semibold mb-4">Mes abonnements</h2>@if($subscriptions->where('status.value','active')->count()>0)<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">@foreach($subscriptions->where('status.value','active') as $sub)<div class="bg-white rounded-xl shadow-sm border p-4"><div class="flex justify-between items-start mb-2"><h3 class="font-semibold text-sm">{{ $sub->magazine->name }}</h3><span class="px-2 py-0.5 bg-green-100 text-green-800 text-xs rounded-full">Actif</span></div><p class="text-sm text-gray-600">{{ $sub->support_type->label() }}</p><p class="text-xs text-gray-500">{{ $sub->start_date->format('d/m/Y') }} - {{ $sub->end_date?$sub->end_date->format('d/m/Y'):'-' }}</p>@if(in_array($sub->support_type->value,['digital','combined']))<a href="{{ route('client.publication',$sub->magazine) }}" class="mt-3 inline-block text-primary-600 hover:underline text-xs">Consulter &rarr;</a>@endif</div>@endforeach</div>@else<div class="bg-white rounded-xl border p-6 text-center text-gray-500"><p>Aucun abonnement actif.</p><a href="{{ route('magazines') }}" class="mt-2 inline-block text-primary-600 hover:underline text-sm">Decouvrir</a></div>@endif</div>
@if(isset($beneficiaries)&&$client->is_payer&&$beneficiaries->count()>0)<div class="mb-8"><h2 class="text-lg font-semibold mb-4">Beneficiaires ({{ $beneficiaries->count() }})</h2><div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">@foreach($beneficiaries as $b)<div class="bg-white rounded-xl shadow-sm border p-4"><p class="font-semibold text-sm">{{ $b->display_name }}</p><p class="text-xs text-gray-500">{{ $b->client_number }}</p>@if($b->activeSubscriptions->count()>0)<div class="flex flex-wrap gap-1 mt-2">@foreach($b->activeSubscriptions as $bs)<span class="px-2 py-0.5 bg-gray-100 text-xs rounded">{{ $bs->magazine->short_name??$bs->magazine->name }}</span>@endforeach</div>@endif</div>@endforeach</div></div>@endif
@if($recentIssues->count()>0)<div class="mb-8"><h2 class="text-lg font-semibold mb-4">Derniers numeros</h2><div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-4">@foreach($recentIssues as $issue)<a href="{{ route('client.issue.read',$issue) }}" class="group bg-white rounded-xl shadow-sm border overflow-hidden hover:shadow-md transition">@if($issue->thumbnail_url)<img src="{{ $issue->thumbnail_url }}" class="w-full h-40 object-cover">@else<div class="w-full h-40 bg-gradient-to-br from-primary-400 to-primary-600 flex items-center justify-center"><span class="text-white text-xs font-bold text-center px-2">{{ $issue->magazine->short_name??$issue->magazine->name }}</span></div>@endif<div class="p-2"><p class="text-xs text-gray-500">{{ $issue->magazine->short_name??$issue->magazine->name }}</p><p class="text-xs font-semibold group-hover:text-primary-600">N{{ $issue->issue_number }}</p></div></a>@endforeach</div></div>@endif</div>
@endsection
