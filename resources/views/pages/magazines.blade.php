@extends('layouts.app')@section('title','Publications')@section('content')
<div class="container-fluid py-12"><div class="text-center mb-12"><h1 class="text-3xl font-bold mb-4">Nos publications</h1></div>@forelse($magazines as $magazine)<div class="bg-white rounded-xl shadow-sm border mb-8 overflow-hidden"><div class="bg-gradient-to-r from-primary-600 to-primary-800 p-6"><h2 class="text-2xl font-bold text-white">{{ $magazine->name }}</h2>@if($magazine->description)<p class="text-primary-100 mt-2">{{ $magazine->description }}</p>@endif</div>@if($magazine->subscriptionPlans->count()>0)<div class="p-6"><div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-4">@foreach($magazine->subscriptionPlans as $plan)<div class="border rounded-lg p-4 hover:border-primary-300 transition"><h3 class="font-semibold text-sm mb-1">{{ $plan->name }}</h3><span class="inline-block px-2 py-0.5 bg-gray-100 text-xs rounded-full mb-2">{{ $plan->support_type->label() }}</span><p class="text-xl font-bold text-primary-600 mb-1">{{ $plan->formatted_price }}</p><p class="text-xs text-gray-500">@if($plan->mode===\App\Enums\SubscriptionMode::DURATION){{ $plan->duration_months }} mois @else {{ $plan->issues_count }} numeros @endif</p></div>@endforeach</div></div>@endif</div>@empty<div class="text-center py-12 bg-white rounded-xl border"><p class="text-gray-500">Aucune publication.</p></div>@endforelse<div class="text-center mt-12 bg-primary-50 rounded-xl p-8"><h2 class="text-2xl font-bold mb-4">Besoin d aide ?</h2><a href="{{ route('contact') }}" class="px-6 py-3 bg-primary-600 text-white rounded-lg font-semibold hover:bg-primary-700">Contactez-nous</a></div></div>
@endsection
